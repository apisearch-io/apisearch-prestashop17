services:
  _defaults:
    public: true

  ##############
  # Your Module

  apisearch.module:
    class: apisearch
    factory: ['Module', 'getInstanceByName']
    arguments:
      - 'apisearch'

  apisearch.context:
    class: Context
    factory: ['Context', 'getContext']

  #####################
  # PrestaShop Account
  apisearch.ps_accounts_installer:
    class: 'PrestaShop\PsAccountsInstaller\Installer\Installer'
    arguments:
      - '5.0'

  apisearch.ps_accounts_facade:
    class: 'PrestaShop\PsAccountsInstaller\Installer\Facade\PsAccounts'
    arguments:
      - '@apisearch.ps_accounts_installer'

  #####################
  # PrestaShop Billing

  apisearch.ps_billings_context_wrapper:
    class: 'PrestaShopCorp\Billing\Wrappers\BillingContextWrapper'
    arguments:
      - '@apisearch.ps_accounts_facade'
      - '@apisearch.context'
      - true # if true, enables the sandbox mode, if false or empty, disables it

  apisearch.ps_billings_facade:
    class: 'PrestaShopCorp\Billing\Presenter\BillingPresenter'
    public: true
    arguments:
      - '@apisearch.ps_billings_context_wrapper'
      - '@apisearch.module'

  # Remove this if you do not need BillingService
  apisearch.ps_billings_service:
    class: 'PrestaShopCorp\Billing\Services\BillingService'
    public: true
    arguments:
      - '@apisearch.ps_billings_context_wrapper'
      - '@apisearch.module'